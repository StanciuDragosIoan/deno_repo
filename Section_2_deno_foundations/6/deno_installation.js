/*
    in order to install deno run:
        curl -fsSL https://deno.land/x/install/install.sh | sh

    then add the 2 below lines to .bashrc in home (/home/amaimon):
        export DENO_INSTALL="/home/amaimon/.deno"
        export PATH="$DENO_INSTALL/bin:$PATH"

*/
