/*
    deno and node are JS runtimes built on top of the v8 engine

    a JS engine takes in JS and spits out instructions for the machine
    it runs on

    a JS runtime is like a wrapper for the engine


*/
